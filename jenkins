pipeline {
    agent any
    
    environment {
        PATH = "/opt/apache-maven-3.8.6/bin:$PATH"
    }

    stages {
        stage('clone') {
            steps {
               
            }
        }
        stage('build') {
            steps {
                sh "mvn clean install"
            }
        }
    }
}
